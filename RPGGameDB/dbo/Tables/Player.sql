CREATE TABLE [dbo].[Player]
(
	[Id] INT NOT NULL PRIMARY KEY IDENTITY, 
    [StrengthPoints] INT NOT NULL, 
    [IntelligencePoints] INT NOT NULL, 
    [CriticalPoints] INT NOT NULL, 
    [ExpPoints] INT NOT NULL
)
